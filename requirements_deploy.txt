# Railway(Linux) 배포용 최소 requirements.
# backend/main.py, backend/routers/*.py, src/chatbot.py, src/llm_config.py 가
# 실제로 import 하는 패키지만 추림 (notebooks/experiments/streamlit 앱/테스트용 패키지 제외).

# ── 웹 서버 ──────────────────────────────────────────────
fastapi==0.136.1
uvicorn==0.47.0
pydantic==2.12.5

# ── 챗봇 / LLM (LangChain + Gemini) ─────────────────────
# langchain-core 버전을 고정하면 langchain-google-genai(>=1.3.2 요구)와
# 충돌하므로, 아래 langchain 계열은 버전 고정을 풀고 pip이 서로 호환되는
# 버전을 알아서 맞추도록 둔다.
python-dotenv==1.2.1
langgraph
langchain-core
langchain-community
langchain-google-genai
langsmith
sentence-transformers==5.6.0
chromadb==1.3.7

# ── 데이터 처리 ──────────────────────────────────────────
pandas==2.3.3
numpy==2.4.0

# ── PDF 보고서 생성 (backend/routers/report.py) ──────────
matplotlib==3.10.7
fpdf2==2.8.7
pillow==12.0.0
